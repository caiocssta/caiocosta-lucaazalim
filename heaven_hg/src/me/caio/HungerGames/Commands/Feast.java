package me.caio.HungerGames.Commands;

public class Feast/* implements CommandExecutor */{
	/*public Main m;

	public Feast(Main m) {
		this.m = m;
	}

	public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
		if (!(sender instanceof Player)) {
			sender.sendMessage(ChatColor.RED + "Voce tem que ser um Player");
		}
		Player p = (Player) sender;
		if (label.equalsIgnoreCase("feast")) {
			if ((GameListener.feast == null) || (!GameListener.feast.isSpawned())) {
				p.sendMessage(ChatColor.RED + "O feast ainda não spawnou.");
				return true;
			}
			p.setCompassTarget(GameListener.feast.central.getLocation());
			p.sendMessage(ChatColor.BLUE + "Bússola apontando para o feast " + ChatColor.RED + "(" + GameListener.feast.central.getX() + ", " + GameListener.feast.central.getY() + ", "
					+ GameListener.feast.central.getZ() + ")");
			return true;
		}
		return false;
	}*/
} 
