package me.skater.CopaHG.listeners;

import org.bukkit.event.Listener;

import me.skater.Main;

public class LoginListener implements Listener {
	// private Main main;

	public LoginListener(Main main) {
		// this.main = main;
	}

	/*
	 * @EventHandler(priority = EventPriority.HIGHEST) public void
	 * onAsyng(AsyncPlayerPreLoginEvent event) { try {
	 * this.main.getCopaManager().loadCopaPlayer(event.getUniqueId()); } catch
	 * (SQLException e) {
	 * event.disallow(AsyncPlayerPreLoginEvent.Result.KICK_OTHER, ChatColor.RED
	 * + "Nao foi possivel carregar grupo da CopaHG."); } }
	 */

}
